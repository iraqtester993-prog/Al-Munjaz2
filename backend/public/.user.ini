; Shared-hosting PHP limits for courier documents and dashboard slide images.
; If the hosting provider enforces a reverse-proxy limit below these values,
; it must raise that proxy limit as well.
upload_max_filesize = 6M
post_max_size = 10M
max_file_uploads = 10
max_input_time = 90
max_execution_time = 120
